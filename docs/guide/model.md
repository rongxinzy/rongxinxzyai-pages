# 模型配置

模型决定知远如何理解任务、生成内容和推进工作。开始使用前，请先配置一个可用的模型。

知远支持配置云端模型，也支持安装和使用本地模型。你可以根据任务类型、响应速度、隐私要求和设备资源选择合适的方式。

## 选择建议

- 需要较强的通用理解能力时，选择适合复杂任务的模型。
- 处理敏感资料或希望在本机完成推理时，可以考虑本地模型。
- 本地模型的可用效果和速度会受到设备资源影响。

具体模型参数和可用选项以应用中的模型设置为准。
