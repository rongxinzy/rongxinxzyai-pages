# 官网重构验证记录

日期：2026-09-05。分支：`codex/editorial-website-redesign`。

## 结果

中英文首页、企业页已切换为工作图谱设计，移除旧版页面、样式和自动动画。原有文档站、发布接口、下载地址校验与历史路由继续使用。构建与本地浏览器验证通过，尚未部署线上。

## 环境

- Codex 内置浏览器，通过 CUA 浏览器接口验证；未使用外部 Playwright 后备方案。
- 开发地址：`http://127.0.0.1:5173/`。
- 生产构建预览：`http://127.0.0.1:4173/`。
- 四条营销路由：`/`、`/en/`、`/enterprise/`、`/en/enterprise/`。
- 布局检查：320、768、1440 像素宽；另做 390 × 844 手机视觉与交互检查。桌面视觉检查视口 1440 × 1000。
- 开发和预览服务器将 `/api/release` 代理至现有官网，只读取公开发布信息。线上 Pages 函数未修改。

## 检查

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 页面身份 | 通过 | 四条路由、语言、标题与单一 h1 对应 |
| React 挂载 | 通过 | `.editorial-site` 存在，静态占位壳已移除 |
| 框架错误界面 | 通过 | 各屏宽均无错误覆盖层 |
| 控制台 | 通过 | 新建生产预览标签页没有 error/warn；故障注入中的网络错误符合预期 |
| 布局 | 通过 | 12 组路由与屏宽检查无页面级横向溢出；手机对比表在自身容器内横向滚动 |
| 演示交互 | 通过 | 资料、表格、代码均完成运行、确认、结果预览与对应文件链接生成 |
| 拒绝与重置 | 通过 | 停止后无生成文件；切换任务恢复初始状态 |
| 键盘 | 通过 | 方向键切换场景；手机导航 Escape 关闭并回到菜单按钮 |
| 下载 | 通过 | 真实清单返回 `2026.9.4-build.2`；Windows、macOS、Ubuntu、AppImage 链接均来自下载域名 |
| 下载降级 | 通过 | 用 CDP 临时阻止本地 `/api/release`，验证失败提示与发布页、安装说明入口，随后解除阻止并重新加载 |
| 企业联系 | 通过 | 联系锚点、邮件链接及两个真实二维码加载正常 |
| 构建 | 通过 | `npm run build`，包含函数类型检查、应用类型检查、Vite 与 VitePress 构建 |
| 补丁格式 | 通过 | `git diff --check` |

## 视觉修订

对照四张概念图检查了实际浏览器截图。保留纸白、墨色与朱红的配色，折纸桥照片、开放式流程、深色边界段及横线下载列表。调整首屏垂直留白、演示正文字号和手机标题断行；平台与文件标识改为原生 SVG。

概念图中自动生成的绝对隐私声明、虚构项目内容与承诺未作为公开事实使用。采用实际本地/云端边界、合同限定、版权主体、二维码及发布数据。演示样例明确标注用途。边界段采用 `03-download.png` 的纵向列表结构。

## 限制

- 未发布到生产环境，未验证线上 CDN、真实域名上的最终交付。
- 安装包只核对发布信息与链接，未在本次设计任务中重新下载或安装。
- 未进行 Safari/Firefox 真机和完整辅助技术审计。减弱动画样式已提供。
- 内置浏览器的全页拼接截图出现重复片段，采用各区段视口截图；DOM 检查确认页面没有重复区段。

## 证据位置

截图保存在本次任务的本地可视化目录：

`/Users/krli/.codex/visualizations/2026/09/05/01a06f60-d183-72f0-9da9-a9b9c2ed9c61/`

- `homepage-desktop.png`
- `homepage-mobile.png`
- `workflow-desktop.png`
- `downloads-desktop.png`
- `enterprise-desktop.png`

图片生成方法与设计规格见同目录 `README.md`；生产图像为 `public/editorial/work-bridge.jpg`。
